[project]
name = "cpp-package-registry-util"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "assertpy>=1.1",
    "pytest>=9.0.2",
    "pytest-sugar>=1.1.1",
]
